import BlogSidebar from '@theme-original/BlogSidebar';

// Pass-through: the Desktop sub-component (src/theme/BlogSidebar/Desktop/) is
// the actual override with the category pills.
export default BlogSidebar;
